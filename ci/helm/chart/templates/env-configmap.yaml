{{- if .Values.env.enable -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mintpad-fe.fullname" . }}-env
  labels:
    {{- include "mintpad-fe.labels" . | nindent 4 }}
data:
  .env: {{ .Values.env.data | quote }}
{{- end }} 