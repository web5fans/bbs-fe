apiVersion: v1
kind: Service
metadata:
  name: {{ include "mintpad-fe.fullname" . }}
  labels:
    {{- include "mintpad-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mintpad-fe.selectorLabels" . | nindent 4 }}
